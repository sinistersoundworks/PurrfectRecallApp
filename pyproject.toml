[project]
name = "ankilikeweb"
version = "0.1.0"
description = "Lightweight spaced-repetition flashcard app (FastAPI + SQLite + vanilla frontend)"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.139.0",
    "python-multipart>=0.0.20",
    "sqlalchemy>=2.0.51",
    "uvicorn>=0.50.2",
]
